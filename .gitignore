node_modules/
dist/
*.db
.dirty-files
.validation-cache.json
.validation-sessions.json
*.log
*.log.old
# Ignore Guardian's runtime output (suggestions.md, caches) but keep committed
# per-project config for the pr-audit skill. Must be `.guardian/*` (not
# `.guardian/`) so the `!` un-ignores below take effect.
.guardian/*
!.guardian/pr-audit.config.json
!.guardian/pr-audit.checklists.md
