"""Domain: fixtures.

Lives under a /tests/ path segment (but the filename itself does NOT match
the test_*.py / *_test.py patterns) — isolates the path-segment skip rule
from the filename-pattern skip rule.
"""


def extra_helper():
    """Domain: fixtures."""
    return None
